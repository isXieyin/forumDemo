<Window x:Class="Wen.WpfApp.View.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Wen.WpfApp"
        mc:Ignorable="d"
        Title="龙云论坛" Height="800" Width="1250"
        FontFamily="Microsoft YaHei" FontWeight="ExtraLight"
        FontSize="12" WindowStartupLocation="CenterScreen"
        WindowStyle="None" AllowsTransparency="True" Background="{x:Null}">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Assets/Styles/DefaultStyle.xaml"/>
                <ResourceDictionary>
                    <Style TargetType="RadioButton" x:Key="NavButtonStyle">
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="RadioButton">
                                    <Border Background="Transparent" CornerRadius="8" x:Name="back">
                                        <ContentControl Content="{TemplateBinding Content}"
                                                        VerticalAlignment="Center"
                                                        HorizontalAlignment="Center"
                                                        Margin="20,4"
                                                        FontSize="13"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsChecked" Value="True">
                                            <Setter TargetName="back" Property="Background" Value="#44FFFFFF"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <!--设置光标 为小手图标-->
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Cursor" Value="Hand" />
                            </Trigger>
                        </Style.Triggers>
                    </Style>

                    <SolidColorBrush x:Key="TextBox.Static.Border" Color="#FFABAdB3"/>
                    <SolidColorBrush x:Key="TextBox.MouseOver.Border" Color="#FF7EB4EA"/>
                    <SolidColorBrush x:Key="TextBox.Focus.Border" Color="#FF569DE5"/>
                    <ControlTemplate TargetType="{x:Type TextBox}" x:Key="SearchTextBoxTemplate">
                        <Border x:Name="border" BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="0" 
                                Background="#22000000"
                                SnapsToDevicePixels="True"
                                CornerRadius="8">   
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="30"/>
                                    <ColumnDefinition/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="&#xe622;" FontFamily="../Assets/Fonts/#iconfont" FontSize="15"
                                           VerticalAlignment="Center"
                                           HorizontalAlignment="Center"
                                           Foreground="#44FFFFFF"/>
                                <TextBlock Text="Search you want" Grid.Column="1" VerticalAlignment="Center" 
                                           Foreground="#44FFFFFF"
                                           Visibility="Collapsed" Name="mt"/>
                                <ScrollViewer x:Name="PART_ContentHost" Focusable="false" Grid.Column="1"
                                              HorizontalScrollBarVisibility="Hidden" 
                                              VerticalAlignment="Center"
                                              VerticalScrollBarVisibility="Hidden"
                                              Background="Transparent"/>        
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter Property="Opacity" TargetName="border" Value="0.56"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource TextBox.MouseOver.Border}"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="true">
                                <Setter Property="BorderBrush" TargetName="border" Value="{StaticResource TextBox.Focus.Border}"/>
                            </Trigger>
                            <DataTrigger Binding="{Binding Text,RelativeSource={RelativeSource Mode=Self}}" Value="">
                                <Setter TargetName="mt" Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>

                    <ControlTemplate TargetType="RadioButton" x:Key="GenderRadioButtonTemplate">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="auto"/>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>
                            <Border Width="14" Height="14" Margin="5,0"
                                    BorderThickness="1" BorderBrush="#007DFA"
                                    CornerRadius="7" VerticalAlignment="Center">
                                <Border Width="8" Height="8" CornerRadius="4" Background="#007DFA"/>
                            </Border>
                            <ContentControl Content="{TemplateBinding Content}" Grid.Column="1"
                                            VerticalAlignment="Center" 
                                            HorizontalAlignment="Center"
                                            Foreground="{TemplateBinding Foreground}"/>
                        </Grid>
                    </ControlTemplate>

                    <Storyboard x:Key="UserInfoStoryboard">
                        <DoubleAnimation Duration="0:0:0:0.5" To="0"
                                         Storyboard.TargetName="t1" Storyboard.TargetProperty="X"/>
                    </Storyboard>
                    <Storyboard x:Key="CloseUserInfoStoryboard">
                        <DoubleAnimation Duration="0:0:0:0.2" To="320"
                                         Storyboard.TargetName="t1" Storyboard.TargetProperty="X"/>
                    </Storyboard>
                </ResourceDictionary>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <!-- 按钮触发动画效果 -->
    <Window.Triggers>
        <EventTrigger RoutedEvent="Button.Click" SourceName="btnUserInfo">
            <BeginStoryboard Storyboard="{StaticResource UserInfoStoryboard}"/>
        </EventTrigger>
        <EventTrigger RoutedEvent="Button.Click" SourceName="btnCloseUserInfo">
            <BeginStoryboard Storyboard="{StaticResource CloseUserInfoStoryboard}"/>
        </EventTrigger>
    </Window.Triggers>

    <Border Margin="5" Background="White" CornerRadius="10">
        <Border.Effect>
            <DropShadowEffect Color="Gray" ShadowDepth="0" BlurRadius="10" Opacity="0.6"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="790"/>
                <RowDefinition Height="51*"/>
            </Grid.RowDefinitions>
            <Border Background="#3D87FF" Height="200" VerticalAlignment="Top" CornerRadius="5,5,0,0"
                    MouseLeftButtonDown="Border_MouseLeftButtonDown"/>
            <!--  窗口控制按钮  Orientation设置hor表示横向布局-->
            <StackPanel Orientation="Horizontal" VerticalAlignment="Top" HorizontalAlignment="Right">
                <Button Style="{StaticResource WindowControButtonStyle}"
                        Content="&#xe60e;" Click="btnMinWindow"/>
                <Button Style="{StaticResource WindowControButtonStyle}"
                        Content="&#xe65b;" Click="btnMaxWindow"/>
                <Button Style="{StaticResource WindowControButtonStyle}"
                        Content="&#xeca0;" Click="btnCloseWindow"/>
            </StackPanel>

            <Grid Margin="50,20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="65"/>
                    <RowDefinition Height="35" />
                    <RowDefinition/>
                </Grid.RowDefinitions>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center">
                    <Image Source="../Assets/Images/heima.png" Width="40" Height="40"/>
                    <StackPanel Orientation="Vertical" Margin="15,0,0,0">
                        <TextBlock Text="龙云音视频播放器" Foreground="White" FontSize="20"/>
                        <TextBlock Text="www.longyun.com" Foreground="#88DDDDDD" FontSize="10"/>
                    </StackPanel>
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Border Width="30" Height="30" CornerRadius="15">
                        <Border.Background>
                            <ImageBrush ImageSource="{Binding UserInfo.HeadPortrait}"/>
                        </Border.Background>
                    </Border>
                    <Button VerticalAlignment="Center" Cursor="Hand"
                            Foreground="White" Margin="10,0,0,0" Name="btnUserInfo">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <TextBlock Text="{Binding UserInfo.UserName}"/>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </StackPanel>

                <!-- 导航栏 -->
                <StackPanel Orientation="Horizontal" Grid.Row="1" HorizontalAlignment="Left" VerticalAlignment="Center">
                    <RadioButton Content="首页" Style="{StaticResource NavButtonStyle}" IsChecked="True"
                                 Command="{Binding NavChangedCommand}"
                                 CommandParameter="FirstPageView"/>
                    <RadioButton Content="视频播放" Style="{StaticResource NavButtonStyle}"
                                 Command="{Binding NavChangedCommand}"
                                 CommandParameter="VideoPageView"/>
                    <RadioButton Content="音乐" Style="{StaticResource NavButtonStyle}"
                                 Command="{Binding NavChangedCommand}"
                                 CommandParameter="MusicPageView"/>
                    <RadioButton Content="关于我们" Style="{StaticResource NavButtonStyle}"/>
                    <RadioButton Content="个人中心" Style="{StaticResource NavButtonStyle}"/>
                </StackPanel>

                <TextBox Grid.Row="1" VerticalAlignment="Center" Width="300" Height="32" 
                         HorizontalAlignment="Right"
                         Template="{StaticResource SearchTextBoxTemplate}"
                         Text="{Binding SearchText}"
                         Foreground="White"/>

                <!-- 主页内容展示 -->
                <ContentControl Grid.Row="2" Content="{Binding MainContent}" Margin="10,0,-10,0"/>
            </Grid>
            <!-- 侧边栏抽屉 -->
            <Border Background="White" Width="300"
                    HorizontalAlignment="Right" 
                    CornerRadius="5,5,5,5">
                <Border.Effect>
                    <DropShadowEffect Color="Gray" Direction="180" ShadowDepth="8" BlurRadius="20" Opacity="0.2"/>
                </Border.Effect>
                <!-- 设置元素位移变形 -->
                <Border.RenderTransform>
                    <TranslateTransform X="320" x:Name="t1"/>
                </Border.RenderTransform>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="230"/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>
                    <Button VerticalAlignment="Top"
                            Margin="10,0,0,0" Name="btnCloseUserInfo" Cursor="Hand">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <TextBlock Text="&#xeca0;" FontFamily="../Assets/Fonts/#iconfont" 
                                            FontSize="20" Margin="0,10,0,0"
                                            Foreground="Gray"/>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                    <Border Width="80" Height="80" CornerRadius="40">
                        <Border.Effect>
                            <DropShadowEffect Color="White" Direction="0" ShadowDepth="0" BlurRadius="5" Opacity="0.2"/>
                        </Border.Effect>
                        <Border.Background>
                            <ImageBrush ImageSource="{Binding UserInfo.HeadPortrait}"/>
                        </Border.Background>
                    </Border>

                    <Grid Grid.Row="1">
                        <Grid.Resources>
                            <Style TargetType="TextBlock">
                                <Setter Property="FontSize" Value="14"/>
                                <Setter Property="FontWeight" Value="Black"/>
                            </Style>
                            <Style TargetType="RadioButton">
                                <Setter Property="FontSize" Value="14"/>
                                <Setter Property="FontWeight" Value="Black"/>
                            </Style>
                        </Grid.Resources>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="40"/>
                            <RowDefinition Height="20"/>
                            <RowDefinition Height="40"/>
                            <RowDefinition Height="40"/>
                        </Grid.RowDefinitions>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="用户昵称:" Margin="10,0,0,0"/>
                            <TextBlock Text="{Binding UserInfo.UserName}" Margin="10,0,0,0"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Grid.Row="1">
                            <TextBlock Text="性别:" Margin="10,0,0,0"/>
                            <RadioButton Margin="10,0,0,0" Template="{StaticResource GenderRadioButtonTemplate}" Content="男"/>
                            <RadioButton Margin="10,0,0,0" Template="{StaticResource GenderRadioButtonTemplate}" Content="女"/>
                        </StackPanel>

                    </Grid>
                </Grid>
            </Border>
           
           
        </Grid>
    </Border>
</Window>
